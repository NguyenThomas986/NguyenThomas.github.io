# Portfolio To-Do

- [ ] Add Open Graph tags so the link preview looks good when shared on LinkedIn
- [x] Add a favicon — the browser tab is blank
- [ ] Add `<main>` landmark tag wrapping the page content
- [ ] Add `prefers-reduced-motion` guard for the pulsing dot animation
- [ ] Fix mobile nav — all links + resume button overflow on small phones
- [ ] Remove dead CSS: `.projects-grid` rule is referenced but never used
- [ ] Remove empty `.delay-1` through `.delay-5` CSS rules or actually implement the staggered animation
- [ ] The hero fade-in classes are static (`opacity: 1`) — no animation actually plays
- [ ] Add `font-display: swap` to Google Fonts to avoid invisible text on slow loads
- [ ] Add `loading="lazy"` to the photo `<img>` tag
